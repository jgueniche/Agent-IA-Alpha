{
  "name": "@alpha/providers",
  "version": "0.0.0",
  "private": true,
  "description": "Interfaces et adaptateurs : abstraction agenda (CalendarProvider) + contrats IA",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"(providers) lint via racine\"",
    "test": "echo \"(providers) tests ajoutes en Phase 4\""
  },
  "dependencies": {
    "@alpha/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
